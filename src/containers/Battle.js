import React from 'react'
import { connect } from 'react-redux'

const Battle = props => {
    return(
        <div>
            <h1>Battle Page!</h1>
        </div>
    )
}
export default connect()(Battle)