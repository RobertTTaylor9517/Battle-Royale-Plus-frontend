import React from 'react'
import { connect } from 'react-redux'

const Characters = props =>{

    const renderChars=()=>{
        for(let i=0;i <= 4;i++){
            
        }
    }

    return(
        <div>
            {renderChars()}
        </div>
    )
}